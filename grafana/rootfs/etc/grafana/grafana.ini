# Minimal grafana.ini; most settings come from GF_* env vars set by the add-on
[server]
# root_url and serve_from_sub_path are set via env for Ingress

[paths]
# Paths are set via env to use /data/* for persistence

[users]
# Prevents welcome flow; optional
default_theme = system
